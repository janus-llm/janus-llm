janus.language.combine
======================

.. py:module:: janus.language.combine


Attributes
----------

.. autoapisummary::

   janus.language.combine.log


Classes
-------

.. autoapisummary::

   janus.language.combine.Combiner
   janus.language.combine.JsonCombiner
   janus.language.combine.ChunkCombiner


Module Contents
---------------

.. py:data:: log

.. py:class:: Combiner(language = 'python')

   Bases: :py:obj:`janus.language.file.FileManager`


   Combine files that have been split into functional blocks back into
   a single file.

   Initialize a FileManager instance.

   :param language: The name of the language to manage.


   .. py:method:: combine(root)
      :staticmethod:


      Combine the given block with its children.

      :param root: The functional code block to combine with its children.



   .. py:method:: combine_children(block)
      :staticmethod:


      Recursively combine block code with children code.

      :param block: The functional code block to recursively replace children.



.. py:class:: JsonCombiner(language = 'python')

   Bases: :py:obj:`Combiner`


   Combine files that have been split into functional blocks back into
   a single file.

   Initialize a FileManager instance.

   :param language: The name of the language to manage.


   .. py:method:: combine(root)
      :staticmethod:


      Combine the given block with its children.

      :param root: The functional code block to combine with its children.



.. py:class:: ChunkCombiner(language = 'python')

   Bases: :py:obj:`Combiner`


   Combine files that have been split into functional blocks back into
   a single file.

   Initialize a FileManager instance.

   :param language: The name of the language to manage.


   .. py:method:: combine(root)
      :staticmethod:


      A combiner which doesn't actually combine the code blocks,
      instead preserving children

      :param root: The functional code block to combine with its children.



