janus.converter.evaluate
========================

.. py:module:: janus.converter.evaluate


Attributes
----------

.. autoapisummary::

   janus.converter.evaluate.log


Classes
-------

.. autoapisummary::

   janus.converter.evaluate.Evaluator
   janus.converter.evaluate.RequirementEvaluator
   janus.converter.evaluate.InlineCommentEvaluator


Module Contents
---------------

.. py:data:: log

.. py:class:: Evaluator(**kwargs)

   Bases: :py:obj:`janus.converter.converter.Converter`


   Evaluator

   A class that performs an LLM self evaluation"
   "on an input target, with an associated prompt.

   Current valid evaluation types:
   ['incose', 'comments']


   Initialize the Evaluator class

   :param model: The LLM to use for translation. If an OpenAI model, the
                 `OPENAI_API_KEY` environment variable must be set.
   :param model_arguments: Additional arguments to pass to the LLM constructor.
   :param max_prompts: The maximum number of prompts to try before giving up.


.. py:class:: RequirementEvaluator(eval_items_per_request = None, input_types = set(['requirements']), output_type = 'requirements_eval', **kwargs)

   Bases: :py:obj:`Evaluator`


   INCOSE Requirement Evaluator

   A class that performs an LLM self evaluation on an input target,
   with an associated prompt.

   The evaluation prompts are for Incose Evaluations


   Initialize the Evaluator class

   :param model: The LLM to use for translation. If an OpenAI model, the
                 `OPENAI_API_KEY` environment variable must be set.
   :param model_arguments: Additional arguments to pass to the LLM constructor.
   :param max_prompts: The maximum number of prompts to try before giving up.


   .. py:method:: translate_block(input_block, failure_path = None)


.. py:class:: InlineCommentEvaluator(eval_items_per_request = None, input_types = set(['cloze_comments']), output_type = 'cloze_comments_eval', **kwargs)

   Bases: :py:obj:`Evaluator`


   Inline Comment Evaluator

   A class that performs an LLM self evaluation on inline comments,
   with an associated prompt.

   Initialize the Evaluator class

   :param model: The LLM to use for translation. If an OpenAI model, the
                 `OPENAI_API_KEY` environment variable must be set.
   :param model_arguments: Additional arguments to pass to the LLM constructor.
   :param max_prompts: The maximum number of prompts to try before giving up.


   .. py:method:: translate_block(input_block, failure_path = None)


