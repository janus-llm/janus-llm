janus.parsers.parser
====================

.. py:module:: janus.parsers.parser


Attributes
----------

.. autoapisummary::

   janus.parsers.parser.log


Exceptions
----------

.. autoapisummary::

   janus.parsers.parser.JanusParserException


Classes
-------

.. autoapisummary::

   janus.parsers.parser.JanusParser
   janus.parsers.parser.GenericParser


Module Contents
---------------

.. py:data:: log

.. py:class:: JanusParser

   Bases: :py:obj:`langchain.schema.output_parser.BaseOutputParser`\ [\ :py:obj:`str`\ ]


   Base class to parse the output of an LLM call.

   Output parsers help structure language model responses.

   .. rubric:: Example

   .. code-block:: python

       class BooleanOutputParser(BaseOutputParser[bool]):
           true_val: str = "YES"
           false_val: str = "NO"

           def parse(self, text: str) -> bool:
               cleaned_text = text.strip().upper()
               if cleaned_text not in (self.true_val.upper(), self.false_val.upper()):
                   raise OutputParserException(
                       f"BooleanOutputParser expected output value to either be "
                       f"{self.true_val} or {self.false_val} (case-insensitive). "
                       f"Received {cleaned_text}."
                   )
               return cleaned_text == self.true_val.upper()

           @property
           def _type(self) -> str:
               return "boolean_output_parser"


   .. py:method:: parse_input(block)

      Parse the input block into raw string input ready to be passed to
      an LLM. Also perform any processing or saving of metadata.

      :param block: The CodeBlock to be processed

      :returns: A parsed version of the input text



   .. py:method:: parse_combined_output(text)

      Parse the output text from the LLM when multiple inputs are combined

      :param text: The output text from the LLM

      :returns: A parsed version of the text



   .. py:method:: parse_into_block(text, block)


.. py:class:: GenericParser

   Bases: :py:obj:`JanusParser`, :py:obj:`langchain_core.output_parsers.StrOutputParser`


   Base class to parse the output of an LLM call.

   Output parsers help structure language model responses.

   .. rubric:: Example

   .. code-block:: python

       class BooleanOutputParser(BaseOutputParser[bool]):
           true_val: str = "YES"
           false_val: str = "NO"

           def parse(self, text: str) -> bool:
               cleaned_text = text.strip().upper()
               if cleaned_text not in (self.true_val.upper(), self.false_val.upper()):
                   raise OutputParserException(
                       f"BooleanOutputParser expected output value to either be "
                       f"{self.true_val} or {self.false_val} (case-insensitive). "
                       f"Received {cleaned_text}."
                   )
               return cleaned_text == self.true_val.upper()

           @property
           def _type(self) -> str:
               return "boolean_output_parser"


   .. py:method:: parse(text)

      Parse a single string model output into some structure.

      :param text: String output of a language model.

      :returns: Structured output.



   .. py:method:: get_format_instructions()

      Instructions on how the LLM output should be formatted.



.. py:exception:: JanusParserException(unparsed_output, *args, **kwargs)

   Bases: :py:obj:`langchain_core.exceptions.OutputParserException`


   Exception that output parsers should raise to signify a parsing error.

   This exists to differentiate parsing errors from other code or execution errors
   that also may arise inside the output parser. OutputParserExceptions will be
   available to catch and handle in ways to fix the parsing error, while other
   errors will be raised.

   :param error: The error that's being re-raised or an error message.
   :param observation: String explanation of error which can be passed to a
                       model to try and remediate the issue. Defaults to None.
   :param llm_output: String model output which is error-ing.
                      Defaults to None.
   :param send_to_llm: Whether to send the observation and llm_output back to an Agent
                       after an OutputParserException has been raised. This gives the underlying
                       model driving the agent the context that the previous output was improperly
                       structured, in the hopes that it will update the output to the correct
                       format. Defaults to False.

   Initialize self.  See help(type(self)) for accurate signature.


