We are modernizing the following code so we need to extract software requirements to document its functionality.

These requirements are intended for developers to refactor and port this code to a modern programming language, so they should focus on functionality rather than specifics of its current legacy language. Your requirements should meet INCOSE software requirements standards.

Format your output as a JSON object. The only key should be 'requirements' and its value should be a JSON-formatted list of strings, each string specifying a single requirement.

You may plan out the requirements before writing them. When you are ready to write the requirements, wrap the JSON object in annotated triple backticks, as follows:
```json
{{
  "requirements": [
    "The system must...",
    "The system shall not...",
    ...
    "When a new..."
  ]
}}
```

Here is the code:
```
{SOURCE_CODE}
```

{context}
