Please convert the following {SOURCE_LANGUAGE} code found in between triple backticks
into {TARGET_LANGUAGE} code.

If the given code is incomplete, assume it is translated elsewhere.
If the given code is missing variable definitions, assume they are assigned elsewhere.
Do not try to improve the provided logic, convert it as faithfully as possible to the target language.

Some more things to remember:
(1) follow standard styling practice for the target language
(2) make sure the language is typed correctly
(3) provide only the code, no additional commentary. The code should be contained in triple backticks.

``` {SOURCE_LANGUAGE}
{SOURCE_CODE}
```
