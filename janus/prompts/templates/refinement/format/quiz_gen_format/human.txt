TASK:
```
{prompt}
```
--------------------
COMPLETION:
```
{completion}
```
--------------------
Above, the COMPLETION should be formatted to have its relevant output in json.
Extract the contents from the COMPLETION, and output a JSON-formatted string where each object has the following keys: "discussion", "question", "option-1", "option-2", "option-3", "option-4", and "correct-answer-number".
Ensure that the output string is properly formatted JSON.

Each JSON object should have the following fields:

    "discussion":"I should assess knowledge about the DFHCOMMAREA because it is important for students to understand how programs use this special memory area to pass data from one program to another program, even if the programs are in different transactions.",
    "question": "What happens if the length of DFHCOMMAREA is invalid?",
    "option-1": "The program terminates",
    "option-2": "The default wait time of 2.5 milliseconds is used",
    "option-3": "An error message is displayed",
    "option-4": "The wait time is set to 900 milliseconds",
    "correct-answer-number": "2"
