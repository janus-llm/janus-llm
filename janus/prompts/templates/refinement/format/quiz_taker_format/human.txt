TASK:
```
{prompt}
```
--------------------
COMPLETION:
```
{completion}
```
--------------------
Above, the COMPLETION should be formatted to have its relevant output in json.
Extract the contents from the COMPLETION, and output a JSON array of objects where each object has the following keys: "question-id", "reasoning", and "selected-answer-number".
Ensure that the output string is properly formatted JSON array.

Each JSON object should have the following fields:

    "question-id": "1",
    "reasoning": "Reasoning here.",
    "selected-answer-number": "2"
