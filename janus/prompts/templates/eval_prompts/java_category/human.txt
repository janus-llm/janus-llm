Please evaluate the following java code using the steps below:

Look through the code and partition the java code into logical blocks (methods, classes, blocks, etc.). Each block should be relatively self-contained and ideally constitute a complete "subroutine", including any associated comments. These blocks should be unique, if one funcitonal block ends on line 46, then next one must begin on line 47. If the code is nested or contains unusually patterns, split it according to your best judgement.

For each block identified, do the following:

1. identify the start line and end line for the functional block. I have appended the source code with line numbers for you to track your blocks.

2. provide an overall assesment of the block, and your reasoning for the assignment. Be succinct, and limit this to a short sentence.

3. assign a label from the following rubric:
    - Non-code Text: Any content that isn't Java code or code comments that would prevent compilation.
    - Lazy Implementation: implementations where the code generation appears to have stopped abruptly, or given up, or comments indicating the implementaiton is incomplete.
    - Placeholder Implementation: Generic placeholder implementations for external system interactions without actual implementation details. Things like mock API calls or simulated responses.
    - Commented Implementation: Functions or sections that describe intended functionality in comments but don't actually implement the described behavior. Example: a function definition, a bunch of comments, then a do nothing.
    - Syntax Error: Java language syntax errors that would prevent compilation. Do not include the line numbers when assigning the syntax error label, I appended those manually to help you track your evaluations.
    - General Error: Logical errors, algorithmic problems, or other non-syntax issues that would prevent the code from working correctly.
    - Clean Implementation: Well-implemented code that does not fall under any of the categories above.  
    
    If multiple labels apply, pick the top label in this list order. For example, a block on Non-code Text may also be a syntax error, but should be labelled as Non-Code Text.

4. provide an integer, between 1-100, evaluation of the code quality of the functional block. Assume that the code surrounding the block is correct and working as expected. Grade only on the provided Java code or text within that block, and use your best judgement, guessing if you have to.


Each block should be evaluated independently based on the above criteria. Your response should be formatted as a list of JSON objects, with each object corresponding to one block. Each object should include five keys: 
`start_line` (integer),
`end_line` (integer),
`section_reasoning` (string), 
`section_labels` (string, from these options: `general_error`, `syntax_error`, `lazy_implementation`, `placeholder_implementation`, `commented_implementation`, `non_code_text`, `clean_implementation`), 
`section_quality` (integer, 1-100). 

Below is an example output for a snippet of java with two functional blocks:
```{EXAMPLE_OUTPUT}```

Evaluate the following java code:
```
{SOURCE_CODE}
```

Don't forget to include your response in JSON format!
